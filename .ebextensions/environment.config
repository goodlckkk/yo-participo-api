# Configuración de variables de entorno para AWS Elastic Beanstalk
# IMPORTANTE: Las variables sensibles (DB_PASSWORD, JWT_SECRET) deben configurarse
# en la consola de AWS o con eb setenv, NO en este archivo

option_settings:
  aws:elasticbeanstalk:application:environment:
    NODE_ENV: production
    PORT: 8080
  
  # Configuración para que use el Procfile
  aws:elasticbeanstalk:container:nodejs:
    ProxyServer: nginx
  
# NOTA: Las siguientes variables deben configurarse en AWS Console:
# - DB_HOST: [tu-rds-endpoint]
# - DB_PORT: 5432
# - DB_USERNAME: admin_yoparticipo
# - DB_PASSWORD: [configurar en AWS Console]
# - DB_DATABASE: yoparticipo_prod
# - JWT_SECRET: [configurar en AWS Console]
# - JWT_EXPIRES_IN: 15m
# - JWT_REFRESH_EXPIRES_IN: 7d
# - FRONTEND_URL: [tu-dominio-frontend]
# - THROTTLE_TTL: 60
# - THROTTLE_LIMIT: 100
